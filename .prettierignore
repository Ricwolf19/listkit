node_modules
dist
build
coverage
pnpm-lock.yaml

# Generated API docs (typedoc)
docs/
**/docs/

# Generated by release-please
CHANGELOG.md
**/CHANGELOG.md

# Package READMEs — hand-formatted on purpose, and the docs site parses them.
# Prettier collapses the elided-code placeholders in the examples, turning
#   defineListConfig<Product>({
#       /* … */
#   })
# into a single unreadable line. lint-staged would do that on any commit that
# touches these files, so they are excluded rather than reformatted.
packages/listkit/README.md
packages/listkit/README.es.md
