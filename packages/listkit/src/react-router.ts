import type { RouterAdapter } from './types/router'

export type { RouterAdapter }

export function reactRouterAdapter(): RouterAdapter {
	throw new Error(
		'reactRouterAdapter is not implemented yet — coming in v0.1.0'
	)
}
