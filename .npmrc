# Scope routing: @pibytelabs packages resolve from company Verdaccio
@pibytelabs:registry=https://registry.alagrandelepusecuca.mx/

# Auth token is injected by CI / set locally via `npm adduser`
# //registry.alagrandelepusecuca.mx/:_authToken=${NPM_TOKEN}

# Workspace behavior
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
